this will only run locally, there are no security concerns, no file will be deleted or modified, only read

settings gear in top right (small gear icon with ~10px padded 'hit box')
 - first setting is a folder path input with a button that opens a system directory selector dialog (i.e., the windows explorer modal to select a folder)
 - second row in settings panel features a 'scan' / 're-scan' button (it says 'scan' if the folder has not yet been scanned, it says 're-scan' if the folder already exists in the sqlite db - scanning lists all files in the folder and all files in all subfolders (recursive/etc). a normalized sqlite db must be constructed to accommodate functionality described later in this document
    [application stores last used/selected directory in the database ('last_used' timestamps stored in a table that foreign_key relates to 'root_dir' table, sort by timestamp, select first, easy peasy)]
 - default mode setting: 'next item shows': [next alphabetical entry, random in root dir, random in current dir]  (user can call the 'next item' in various ways, these are not settings, they all just work: right click or right arrow key. 'previous item': left click or left arrow key)
 - default 'slideshow' mode settings: 'next item shows': [next alphabetical entry, random in root dir, random in current dir] (same as above). slide duration setting: time in seconds, integer input field. when in slideshow mode previous/next inputs still work, reset duration clock on each input, previous goes to previous item in slideshow meaning we'll need to store a list of indices as they are generated for each slideshow session - a 'start/stop/pause' slideshow toolbar should be on the bottom right - stop erases list of indices, pause retains and allows resumption and/or navigation with prev/next inputs. 
    [all toolbar buttons are made invisible unless cursor is in the hitbox (again padded ~10px)]
- available tags: short strings can be added to a list of 'tags' that can be added to each item, items can carry multiple tags, the tag interface will be on the bottom left corner of the display (hidden until cursor hits region) - the tag interface shows a list of tags the item currently has associated with it (vertical list) with a dropdown/select menu below the list and an "add" button next to the dropdown - when adding a tag it automatically gets put in a tag table in the sqlite db associated with the item's foreign key, and each tag when hovered over provides an 'x' button to remove it from the table and the list.
- tag images : getting a bit more complicated here, while the tag list is hidden until the cursor hits the region, we should also have the option to have small images and/or sound files associated with each tag - such that when an item is displayed, the tag images appear in the top left, and the sounds play (if multiple sounds, they play sequentially, if multiple images, they display from left to right) - the selection/addition/removal options for tag images and sounds should be incorporated into the settings panel with the global tag manager.

---
clarification text:

this system should create a relatively universal media viewer (images and videos) that lets users browse manually or watch slideshows of their media, associate tags with media items that can also be associated with little 'badges' (the tag images) or sounds that play when certain media items come up - making the enjoyment of media files a bit more multi-dimensional and fun.

I was thinking that this should be a web application, but I wonder, this will run on windows 11 - it should be able to play any video file that vlc media player can play, and view any image file that irfanview can view (I suspect both of these applications use open source libraries under the hood, but I don't want things to get too bloated, so a bit unsure what the ideal architecture would be) - can this be a 'multimedia player' written in some easily compiled (or no-compile) windows-compatible language? a python application might do the trick, but might also bring a lot of unneeded bloat - before you begin working or planning, give me some feedback/guidance and options on what ~language the application should be written in. (fullscreen media player/viewer, with ui elements/overlays, layered sounds (e.g., a video file with a sound file associated with a tag)). again, maybe a web application would be best since the user already has the bulky/bloated web browser capable of doing everything - but I wanted to throw it out there as a question/suggestion that maybe this could be something more standalone and robust for the intended purpose. let me know what you think